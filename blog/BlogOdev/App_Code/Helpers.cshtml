@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<BlogOdev.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@BlogOdev.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}