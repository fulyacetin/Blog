@model IList<BlogOdev.Core.Objects.Post>

<div class="sidebar">
  <h3>Son Mesajlar</h3>

  @if (Model.Count > 0)
  {
    <ul>
      @foreach (var post in Model)
      {
        <li>@Html.PostLink(post)</li>
      }
    </ul>
  }
  else
  {
    <p>Hiç mesaj bulunamadı!</p>
  }
</div>