@model IList<BlogOdev.Core.Objects.Category>

<div class="sidebar">
  <h3>Kategoriler</h3>

  @if (Model.Count > 0)
  {
    <ul>
      @foreach (var category in Model)
      {
        <li>@Html.CategoryLink(category)</li>
      }
    </ul>
  }
  else
  {
    <p>Hiçbir kategori bulunamadı!</p>
  }
</div>