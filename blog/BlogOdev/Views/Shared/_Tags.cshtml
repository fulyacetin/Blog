@model IList<BlogOdev.Core.Objects.Tag>

<div class="tags sidebar">
  <h3>Etiketler</h3>

  @if (Model.Count > 0)
  {
    foreach (var tag in Model)
    {
      @Html.TagLink(tag)
    }
  }
  else
  {
    <p>Etiket bulunamadı!</p>
  }
</div>
